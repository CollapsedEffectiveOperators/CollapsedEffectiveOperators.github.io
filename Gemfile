source "https://rubygems.org"

# Pinned to the Jekyll already installed in this environment (4.4.x).
gem "jekyll", "~> 4.4"

# WEBrick was removed from Ruby's standard library in 3.0, but `jekyll serve`
# still needs it for the local preview server.
gem "webrick", "~> 1.8"
